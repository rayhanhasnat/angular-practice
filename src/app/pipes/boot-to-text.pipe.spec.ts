import { BootToTextPipe } from './boot-to-text.pipe';

describe('BootToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new BootToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
